#Vale CABE ACLARAR, los arreglos como tal no existen en python
#pero tratare de explicar el modo de utilzarlos en este lenguaje de igual modo
#Como se sabe y similar a la lista(esto es lo mas cercano a un arreglo en este lenguaje) se inicia declarandolo
# AL IGUAL QUE LA LISTA INICIA EN 0

#este es un arreglo del tipo vector, ya que este solo contiene una dimension
#          0 1 2
arreglo = [1,2,3]
#pero que son las dimensiones?

#las dimensiones son las zonas que existen dentro de un arreglo
#Estas son generalmente tambien llamadas celdas
#hay varios tipos de arreglos, los comunes son del tipo vector y matriz

#EL ARREGLO VECTOR
#Un arreglo vector es un tipo de vector de una sola dimension, tal como la variable arreglo declarada
#anteriormente, esta almacena objetos pero solo al nivel de una dimension.

#EL ARREGLO MATRIZ
#Este ya difiere un tanto del arreglo vector, ya que este tipo de vector posee 2 o mas dimensiones
#al hacer esto es capaz de almacenar una cantidad mas grandes de datos y mejor administradas.

#Ejemplo
#            0       1       2
matriz = [[1,3,2],[6,5,4],[8,9,0]]

#Te invito a que hagas tus propios programas para que vayas viendo como utilizarlos a tu antojo